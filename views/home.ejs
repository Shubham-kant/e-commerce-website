<link rel="stylesheet" href="/css/home.css">
<% for(var i of products){  %>
    <img src="<%= i.avatar %>" alt="<%= i.name %>" width="100" height="100">
    <%= i.name %>
    <%= i.price %>
    <!-- <%= i.description %> -->
    <!-- <%= i.type %> -->
    <!-- <%= i.subtype %> -->
    <!-- <%= i.manufacturer %> -->
    <!-- <%= i.stock %> -->
<% } %>